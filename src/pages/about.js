import React from "react";
import Layout from "../components/Layout";

const About = () => {
    return (
        <div>
            <Layout>
                <div className="about">
                    about-page
                </div>
            </Layout>
        </div>
    );
};

export default About;
